name = "pickup-worker"
main = "src/index.ts"
compatibility_date = "2026-06-01"

# Set with: wrangler secret put SUPABASE_URL / SUPABASE_ANON_KEY / ADMIN_TOKEN
# ANON key only — the worker reads public event data through RLS, never the service role.

[vars]
SITE_ORIGIN = "https://pickupsports.app"
